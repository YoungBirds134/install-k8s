apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf-rabbit-mq
  namespace: message-queue
data:
  nginx.conf: |
    user nginx;
    worker_processes  1;
    events {
      worker_connections  10240;
    }
    http {
      upstream rabbit {
        server 192.168.1.11:15672;
      }
      server {
        listen       80;
        server_name  localhost;
        location / {
          #root   /usr/share/nginx/html; #Change this line
          #index  index.html index.htm;
          proxy_pass http://rabbit;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
          try_files $uri $uri/ /index.html =404;
          root /folder-with-rabbit-mq-connect-ui/;
        }
      }
    }

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbit-mq-nginx
  namespace: message-queue
spec:
  selector:
    matchLabels:
      app: rabbit-mq-nginx
  replicas: 1
  template:
    metadata:
      labels:
        app: rabbit-mq-nginx
    spec:
      containers:
      - name: rabbit-mq-nginx
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
            - name: nginx-conf-rabbit-mq
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
      volumes:
      - name: nginx-conf-rabbit-mq
        configMap:
          name: nginx-conf-rabbit-mq
          items:
            - key: nginx.conf
              path: nginx.conf

---
apiVersion: v1
kind: Service
metadata:
  name: rabbit-mq-service
  namespace: message-queue
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 80
  selector:
    app: rabbit-mq-nginx
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: message-queue
  name: rabbit-mq-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    #nginx.ingress.kubernetes.io/rewrite-target: /$2
    cert-manager.io/issuer: "letsencrypt-prod"
spec:
  tls:
   - hosts:
      - rabbit-mq.congcu24h.com #domain
     secretName:  test-domain-tls
  rules:
  - host: rabbit-mq.congcu24h.com #domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rabbit-mq-service
            port:
              number: 8080
